# Add a new user (replace 'fedora' with your desired username)
adduser fedora

# Set a password for the new user (replace 'newpassword' with your desired password)
echo "fedora:newpassword" | chpasswd
